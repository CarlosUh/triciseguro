@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">Promocion_aplicada {{ $promocion_aplicada->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/promocion_aplicada') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/promocion_aplicada/' . $promocion_aplicada->id . '/edit') }}" title="Edit Promocion_aplicada"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('promocion_aplicada' . '/' . $promocion_aplicada->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete Promocion_aplicada" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $promocion_aplicada->id }}</td>
                                    </tr>
                                    <tr><th> Promocion Id </th><td> {{ $promocion_aplicada->promocion_id }} </td></tr><tr><th> Solicitud Id </th><td> {{ $promocion_aplicada->solicitud_id }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
